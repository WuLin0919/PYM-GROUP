# Archive

This folder contains historical member workbooks, spreadsheet previews, and inspection records.
These files are retained for traceability and are not read by the website.

Use `source_data/members/group_members_master.xlsx` as the editable member reference workbook,
and update `_data/members.yml` when changes need to appear on the website.

